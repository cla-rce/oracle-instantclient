#!/bin/sh

case "${SSH_ORIGINAL_COMMAND}" in
  *\&*)
    echo "Rejected"
    ;;
  *\(*)
    echo "Rejected"
    ;;
  *\{*)
    echo "Rejected"
    ;;
  *\;*)
    echo "Rejected"
    ;;
  *\<*)
    echo "Rejected"
    ;;
  *\`*)
    echo "Rejected"
    ;;
  *\|*)
    echo "Rejected"
    ;;
  rsync\ --server\ --sender\ -vlogDtprze.iLsf\ .\ /db/instances/staging/data/repstaging/*)
    ${SSH_ORIGINAL_COMMAND}
    ;;
  *)
    echo "Rejected"
    ;;
esac 
