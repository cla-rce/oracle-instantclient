## forward all messages to syslog
<% node[:cla_unix_baseline][:syslog_servers].each do |server| %>
<% server.match('(:\d+)') ? (port = "") : (port = ":514") %>
*.info   				  @<%= server %>
<% end %>

